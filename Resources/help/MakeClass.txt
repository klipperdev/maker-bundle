The <info>%command.name%</info> command generates a new generic class.

<info>php %command.full_name% MyNamespace\MyClass</info>

If the argument is missing, the command will ask for the choice class interactively.
