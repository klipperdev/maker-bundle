The <info>%command.name%</info> command generates a new choice class.

<info>php %command.full_name% PostStatus</info>

If the argument is missing, the command will ask for the choice class interactively.
